version: 1

deploy:
  steps:
    setupVariables:
      after:
        - npm i
        - pulumi config set aws:region us-east-1

destroy:
  steps:
    setupVariables:
      after:
        - npm i
        - pulumi config set aws:region us-east-1        
