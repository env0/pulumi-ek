version: 1

deploy:
  steps:
    setupVariables:
      after:
        - npm i
    pulumiLogin:
      after:
        - pulumi config set aws:region us-east-1

destroy:
  steps:
    setupVariables:
      after:
        - npm i
    pulumiLogin:
      after:
        - pulumi config set aws:region us-east-1
